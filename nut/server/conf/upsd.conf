#
# upsd.conf (server)
# NUT network server configuration
#

# Listen on localhost and LAN interface as needed
LISTEN 127.0.0.1 3493
# LISTEN 192.168.1.2 3493

# Max connections can be tuned for larger environments
MAXAGE 25
