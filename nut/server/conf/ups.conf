#
# ups.conf (server)
# UPS device configuration
#

[ups]
  driver = usbhid-ups
  port = auto
  desc = "APC Back-UPS Pro 900 (example)"

  # Delay before power cut after shutdown command (seconds)
  # Tune to allow Raspberry Pi to shut down cleanly.
  offdelay = 60
